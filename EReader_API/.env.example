# Copie para .env e ajuste. O .env NÃO é versionado.
# Consumido por: docker compose -f EReader_API/docker-compose.yml up
# (interpolação de ${VAR} no docker-compose.yml)

POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=ereader
